# dependencies (reinstall with `npm i`; CI installs ajv itself)
node_modules/
# build / generated
dist/
build/
next/build/shacl/*
!next/build/shacl/.gitkeep
# logs / temp / OS
*.log
*.tmp
*.orig
.DS_Store
Thumbs.db
# local env
.env
.env.*
# agent session dir + npm lock (deps are reinstalled, not pinned — see top)
.claude/
package-lock.json
