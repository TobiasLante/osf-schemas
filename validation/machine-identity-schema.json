{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "machine-identity-schema.json",
  "title": "Machine identity registry",
  "description": "Per-BUS-machine identity facts that no other SSOT file states: the ERP machine_ref a machine's orders are booked under, and whether the id mirrors another machine. Every entry must carry the measurement that established it — see mappings/machine-identity.json.",
  "type": "object",
  "additionalProperties": false,
  "required": ["mapId", "version", "description", "machines"],
  "properties": {
    "mapId": { "const": "machine-identity" },
    "version": { "type": "string", "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$" },
    "description": { "type": "string", "minLength": 1 },
    "machines": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/definitions/machineIdentity" }
    }
  },
  "definitions": {
    "machineIdentity": {
      "type": "object",
      "additionalProperties": false,
      "required": ["busMachineId", "erpMachineRef", "erpMachineRefAliases", "mirrorOf", "evidence"],
      "properties": {
        "busMachineId": {
          "type": "string",
          "minLength": 1,
          "description": "The id telemetry is published under (machine_events.machine) — the id every consumer query must use."
        },
        "erpMachineRef": {
          "type": ["string", "null"],
          "minLength": 1,
          "description": "The ERP machine_ref this machine's production orders are booked under NOW. NULL means no binding could be PROVEN; a consumer must then leave the order binding absent rather than guess a similar name."
        },
        "erpMachineRefAliases": {
          "type": ["array", "null"],
          "items": { "type": "string", "minLength": 1 },
          "minItems": 1,
          "uniqueItems": true,
          "description": "Refs the SAME machine's orders were booked under BEFORE the current one — the trail of an ERP-side rename. A consumer MUST join on the union of erpMachineRef and these, because a retrospective window is longer than the rename: binding on the current ref alone greys every bucket before the flip, binding on the old ref alone greys every bucket after it, and both report a rename as an absent order. NULL means the ref has never changed (the common case) — an empty array is not allowed, so 'no aliases' cannot be confused with 'nobody looked'. Each alias must be justified in evidence.measured like any other identity claim."
        },
        "mirrorOf": {
          "type": ["string", "null"],
          "minLength": 1,
          "description": "When set, this id is a second protocol view of the SAME physical machine, already present under the named busMachineId. Mirrors must be excluded from roll-up fan-out (double counting) and from learning (folding one reality in twice narrows the learned variance artificially)."
        },
        "evidence": {
          "type": "object",
          "additionalProperties": false,
          "required": ["measured", "on"],
          "properties": {
            "measured": {
              "type": "string",
              "minLength": 40,
              "description": "What was measured, with the numbers. Required and deliberately long: an identity justified by name similarity alone is a bug even when it happens to be right."
            },
            "on": { "type": "string", "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$", "description": "The date the measurement was taken (YYYY-MM-DD). A pattern, not format:date — ajv-formats is not a dependency here and an ignored format is a check that silently does nothing." }
          }
        },
        "note": {
          "type": "string",
          "minLength": 1,
          "description": "An open discrepancy between this declaration and something else in the repo or the plant — kept so a human resolves it instead of a consumer quietly overriding it."
        }
      }
    }
  }
}
