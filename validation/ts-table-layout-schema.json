{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://osf-schemas/validation/ts-table-layout-schema.json",
  "title": "TS Table Layout",
  "description": "Meta-schema for historians/central-ts-tables/*.json. Declarative per-MachineType table-shape consumed by render-ddl.py to emit CREATE TABLE + hypertable + indexes + retention idempotently. Each table strictly telemetry XOR event XOR child.",
  "type": "object",
  "required": ["machineType", "profileRef", "tables"],
  "properties": {
    "machineType": { "type": "string", "minLength": 1 },
    "profileRef": { "type": "string", "pattern": "^SMProfile-" },
    "tables": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["name", "primaryKey", "columns"],
        "properties": {
          "name": { "type": "string", "minLength": 1 },
          "primaryKey": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string", "minLength": 1 }
          },
          "columns": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "object",
              "required": ["name", "type"],
              "properties": {
                "name": { "type": "string", "minLength": 1 },
                "type": { "type": "string", "minLength": 1 }
              },
              "additionalProperties": true
            }
          },
          "indexes": {
            "type": "array",
            "items": {
              "type": "object",
              "required": ["name", "columns"],
              "properties": {
                "name": { "type": "string", "minLength": 1 },
                "columns": { "type": "string", "minLength": 1 }
              },
              "additionalProperties": true
            }
          }
        },
        "additionalProperties": true
      }
    }
  },
  "patternProperties": {
    "^\\$": { "type": "string" }
  },
  "additionalProperties": false
}
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://osf-schemas/validation/ts-table-layout-schema.json",
  "title": "TS Table Layout",
  "description": "Meta-schema for historians/central-ts-tables/*.json. Declarative per-MachineType table-shape consumed by render-ddl.py to emit CREATE TABLE + hypertable + indexes + retention idempotently. Each table strictly telemetry XOR event XOR child.",
  "type": "object",
  "required": ["machineType", "profileRef", "tables"],
  "properties": {
    "machineType": { "type": "string", "minLength": 1 },
    "profileRef": { "type": "string", "pattern": "^SMProfile-" },
    "tables": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["name", "primaryKey", "columns"],
        "properties": {
          "name": { "type": "string", "minLength": 1 },
          "primaryKey": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string", "minLength": 1 }
          },
          "columns": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "object",
              "required": ["name", "type"],
              "properties": {
                "name": { "type": "string", "minLength": 1 },
                "type": { "type": "string", "minLength": 1 }
              },
              "additionalProperties": true
            }
          },
          "indexes": {
            "type": "array",
            "items": {
              "type": "object",
              "required": ["name", "columns"],
              "properties": {
                "name": { "type": "string", "minLength": 1 },
                "columns": { "type": "string", "minLength": 1 }
              },
              "additionalProperties": true
            }
          }
        },
        "additionalProperties": true
      }
    }
  },
  "patternProperties": {
    "^\\$": { "type": "string" }
  },
  "additionalProperties": false
}
