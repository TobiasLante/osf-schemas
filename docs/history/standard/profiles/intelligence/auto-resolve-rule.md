# profiles/intelligence/auto-resolve-rule.json — history and reasoning

Moved verbatim out of `profiles/intelligence/auto-resolve-rule.json`, where each field now holds its first sentence. The JSON says what a thing is; this file keeps why it became that.

## description

next/ v2.0 — intelligence-engine configuration object: a rule that auto-resolves or proposes resolutions for discrepancies. Stub so Discrepancy.RESOLVED_BY_RULE and ResolutionProposal.PROPOSED_BY_RULE resolve. Validated by the canonical intelligence schema (out of scope for the unified profile-schema).
