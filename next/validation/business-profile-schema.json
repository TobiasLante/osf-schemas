{
 "$schema": "http://json-schema.org/draft-07/schema#",
 "$id": "https://osf-schemas/next/validation/business-profile-schema.json",
 "title": "OSF Business SM Profile — next/ (SHIM -> profile-schema.json) — v2.0",
 "description": "SHIM. The machine/equipment/business meta-schemas were unified into ONE next/validation/profile-schema.json keyed on the (now-required) 'category'. This file is kept (same $id) so consumers that load by filename keep working; it just $refs the unified schema. A business profile must additionally set category:'business' — enforced by the unified schema's if/then branch (requires isa95.objectModel; allows isKey/isId on attributes). Validate by registering profile-schema.json + constraint-schema.json alongside this shim.",
 "allOf": [
 { "$ref": "profile-schema.json" },
 { "properties": { "category": { "const": "business" } } }
 ]
}
